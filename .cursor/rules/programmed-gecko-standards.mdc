---
description: programmed-gecko 项目编码规范与修改范围约束
alwaysApply: true
---

# programmed-gecko 编码规范

## 内聚与耦合

- 遵循高内聚、低耦合：相关逻辑放在同一类/模块内，模块间通过清晰接口通信。
- 不为追求原则而过度抽象：若拆分后反而更难理解或维护，保持简单直接的实现。

```cpp
// ❌ 过度拆分：仅为“解耦”引入多余层次
class FootPositionCalculatorFactory { ... };

// ✅ 适度内聚：职责清晰，结构可读
class Foot {
    void updatePosition(const BodyChain& body);
};
```

## 面向对象

- 善用 OOP 表达领域概念（如 BodyChain、Foot、Application），让类名与职责一目了然。
- 代码应具教学价值：结构清晰、命名准确，便于读者理解设计意图，而非炫技。

## 注释

- 降低注释密度；代码本身应能说明大部分逻辑。
- 仅在必要时添加：函数/类的功能说明，以及非显而易见的参数含义。

## 项目约束

- 本项目为 **Visual Studio** 工程，修改后须能在 VS 中成功生成并运行。
- **修改范围仅限** `src/` 目录下的源代码（`.cpp`、`.h` 等）和 JSON 配置文件。
- 不要修改 `programmed-gecko.vcxproj`、解决方案文件、预编译头配置等项目文件，除非用户明确要求。
